{
  "compilerOptions": {
    // 目标代码（ts -> js(es5/6/7)）
    "target": "esnext",
    // 目标代码需要使用的模块化方案
    "module": "esnext",
    // 严格检查
    "strict": true,
    // 对jsx进行处理（preserve保留）
    "jsx": "preserve",
    // 辅助导入工功能
    "importHelpers": true,
    // 按照node方式去解析模块
    "moduleResolution": "node",
    // 跳过对整个lib库的类型检测
    "skipLibCheck": true,
    // export default / module.export ={}  允许混合使用
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    // 要不要生成映射文件(ts -> js)
    "sourceMap": true,
    // 文件路径在解析时，基本url
    "baseUrl": ".",
    // 指定具体要解析使用的配型
    "types": ["webpack-env"],
    // 编译阶段的路径解析
    "paths": {
      "@/*": ["src/*"],
      "components/*": ["src/components/*"]
    },
    // 可以指定在项目中可以使用哪里的库的类型(Proxy/Window/Document)
    "lib": ["esnext", "dom", "dom.iterable", "scripthost"]
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.vue",
    "tests/**/*.ts",
    "tests/**/*.tsx"
  ],
  "exclude": ["node_modules"]
}
